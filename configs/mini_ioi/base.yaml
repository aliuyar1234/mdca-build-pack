setting:
  id: mini_ioi
  variant: base
  description: Smoke config for the miniature IOI S2 setting.
paths:
  results_dir: results
  artifacts_dir: artifacts
method:
  bootstrap_n_reps: 1000
  null_bin_width_bits: 2
  null_min_family_count: 5
  frontier_balance_seed: 0
seeds:
  global: 10
  dataset: 10
  model_init: 10
  candidate_search: 10
  bootstrap: 10
runtime:
  device: cpu
  dtype: float32
  num_workers: 0
extras:
  phase: smoke
  setting_scope: S2
  transformer_training:
    epochs: 150
    batch_size: 16
    learning_rate: 3.0e-3
    weight_decay: 0.0
